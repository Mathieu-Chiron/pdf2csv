# pdf2csv

The goal of this project is to convert PDF files into CSV files.

It extracts structured data (such as tables, forms, or text) from PDF documents
and outputs them as CSV files for easy use in spreadsheets and data analysis tools.
